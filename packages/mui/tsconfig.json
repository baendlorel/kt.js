{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@ktjs/core",
    "rootDir": ".", // This is for "../shared/src/**/*"
    "types": ["node", "../types/macros"]
  },
  "include": ["./src/**/*"]
}

// TODO mui的打包最好要做到一个组件一个文件，这样才能避免直接injectGlobal。
