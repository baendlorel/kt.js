# @ktjs/transformer

Shared JSX transform core used by KT.js integration packages.

- Exports Babel plugin factory (`default`)
- Exports `transformWithKTjsx` helper
- Contains directive transforms for `k-if`/`k-else` and `k-for`
