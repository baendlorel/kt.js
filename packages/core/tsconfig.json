{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@ktjs/shared": ["../shared/index.ts"] // share common folder
    }
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"], // include tests
  "references": [{ "path": "../shared" }] // share common folder
}
