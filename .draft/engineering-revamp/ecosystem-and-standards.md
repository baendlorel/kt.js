# 生态与规范

## 目标
让第三方库具备一致的可预测行为与接入方式。

## 建议
- 发布组件作者指南，明确生命周期与作用域规则。
- 提供标准工具：事件辅助、清理辅助、调度辅助。
- 统一 props/children/ref 的契约。
- 提供官方小型组件库作为参考实现。

## 工具链
- 针对 `ref` 使用与 `onUnmount` 清理提供 lint 规则。
- 未清理事件监听时在 dev 模式给出警告。
