{
  "compilerOptions": {
    "types": ["node"], // # This cannot be omitted. Or you will get a warning.
    "rootDir": "."
  },
  "include": ["./src/**/*.ts"]
}
// FIXME 虽然可以打包了，但是作为ts-plugin它必须以commonjs的require加载。
