# 架构说明

## 不依赖虚拟 DOM
KT.js 可以用真实 DOM + 作用域模型实现生命周期管理。作用域挂载在真实节点上，节点移除时触发清理，从而达到“可靠性”目标而不引入 VDOM。

## 作用域挂载点
- 元素创建：`h()` 与 `jsx()`。
- 条件渲染：`k-if` 交换节点。
- 列表更新：`KTFor` 批量替换。

## 清理钩子
- 事件监听与响应式订阅进入作用域。
- 框架工具替换节点时触发作用域销毁。
